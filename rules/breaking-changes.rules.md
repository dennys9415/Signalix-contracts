# Breaking Change Rules

Breaking changes:
- removing fields
- renaming fields
- changing enum values
- removing enum values
- changing event names
- changing payload required fields
- changing API response structure

Safe changes:
- adding optional fields
- adding new enum values with version note
- adding new events
- adding new DTOs

Use semantic versioning.
